<!DOCTYPE html>
<html>
<head>
  <title>CardGame</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class="body">

<div class="admin-panel">
  <div class="admin-panel-discr">
    <h4> ADMIN  </h4>
  </div>
  <div class="admin-panel-button" >
    <%= link_to "Панель Администратора", "admin", class: "btn btn-default" %>
  </div>
</div>

<div>
  <% if user_signed_in? %>
    Вы вошли как <%= current_user.name.capitalize %>.
    <%= link_to "Выход", destroy_user_session_path, :method => :delete %>
  <% else %>
    <%= link_to "Зарегистрироваться", new_user_registration_path %> или <%= link_to "Войти", new_user_session_path %>
    <% end %>
</div>
 
<%= yield %>

</body>
</html>
